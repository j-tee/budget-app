<tr>
    <th scope="row">#<%= category.id %></th>
    <td><%= category.icon %></td>
    <td class="amount"><%= category.total_amount %></td>
    <td class="name"><%= link_to category.name, category_transaction_items_path(category.id), method: :get, class:"nav-link" %></td>
</tr>
